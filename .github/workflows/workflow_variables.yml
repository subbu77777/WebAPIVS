name: Testing workflow Variables

on:
    workflow_dispatch:
env:
    cloud: google-cloud

jobs:
    greeting-job:
        runs-on: ubuntu-latest
        env:
            greeting: Hello
        steps:
          - name: "say hello"
            run: echo "$greeting $subbarao is using $cloud"
            env:
                first_name: subbarao
                






  