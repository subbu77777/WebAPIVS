# This file is used to ignore files that are generated by the build process, IDEs, or testing frameworks.

# --------------------
# 1. Project & Build Outputs
# --------------------
# Binaries (intermediate and final)
[Dd]ebug/
[Rr]elease/
[Bb]in/
[Oo]bj/

# Test results
*teststore/
*.trx
*.vsix
*.pdb
*.suo

# NuGet packages
*.nupkg
# The packages directory may be used when TFM is not netcoreapp
packages/
# 'project.assets.json' is a file generated by NuGet during restore.
# It should usually NOT be ignored, but sometimes local build issues can occur.
# Uncomment the line below if you prefer to ignore it (use with caution):
# **/obj/project.assets.json

# --------------------
# 2. Visual Studio & IDE Artifacts
# --------------------
# Visual Studio solution user options (local settings)
*.sln.docstates
*.vsp
*.vuetest
*.vshost.*
*.user
*.useros
*.tlog
*.log
_ReSharper.*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# Visual Studio Code (VS Code) specific files
.vscode/launch.json
.vscode/tasks.json
# VS Code extensions often create workspace data
.vscode/settings.json
.vscode/*.code-workspace

# Rider / JetBrains IDE files
.idea/

# --------------------
# 3. Application & Deployment
# --------------------
# IIS Express files
##/*.vs

# Local application secrets/settings (crucial for security)
# This prevents accidentally committing credentials, API keys, etc.
appsettings.Development.json
appsettings.Local.json
launchsettings.json
# Exclude all files named 'secrets.json' in any folder (ASP.NET Core secret storage)
**/secrets.json

# Azure/Cloud deployment artifacts
*.publishproj
*.pubxml

# --------------------
# 4. Operating System Files
# --------------------
# Windows
Thumbs.db
Desktop.ini

# macOS
.DS_Store